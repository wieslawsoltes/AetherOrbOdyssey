# GitHub import provenance

The original 42 text files were transported without edits in a SHA-256-verified archive. The exact text baseline is retained in `source-baseline.tar.xz`; the Pages workflow is installed separately by the connected account.

The repository has new import commits. It does not reproduce the earlier local Git history. The earlier ZIP and history bundle remain separate original handoff artifacts.

The original PNG/JPEG preview bytes were not transferred. All ten preview frames and their contact sheet were regenerated from the unchanged application by this GitHub Actions run. `tests/browser-results.json` is the new run report. The older QA and publication documents are historical.

Browser verification uses the explicitly labelled WebGL2 compatibility renderer in Chromium/SwiftShader. It does not establish native WebGPU or physical-device performance. The audio transport fixture is discarded; the actual credited recording is fetched and verified separately by the Pages build.

Import run: https://github.com/wieslawsoltes/AetherOrbOdyssey/actions/runs/35113099680
